server:
  port: 8911
  context-path: /c-b

eureka:
  client:
    serviceUrl:
          defaultZone: http://localhost:8010/eureka/

spring:
  application:
      name: client-b

#该参数不为true，监控页面根本打不开的
feign:
  hystrix:
    enabled: true